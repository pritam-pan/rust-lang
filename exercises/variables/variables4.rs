// variables4.rs
//
// Execute `rustlings hint variables4` or use the `hint` watch subcommand for a
// hint.


fn main() {
    let mut x = 3;
    //by default x is immutable variable so make it mutable we have to add mut keyword
    println!("Number {}", x);
    x = 5; // don't change this line
    println!("Number {}", x);
}
